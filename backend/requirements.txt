fastapi==0.115.12
uvicorn==0.34.0
python-multipart==0.0.20
google-genai==1.14.0
httpx==0.28.1
supabase==2.13.0
pydantic==2.11.1
pydantic-settings==2.8.1
python-dotenv==1.1.0
python-barcode==0.15.1
Pillow==11.1.0
pytesseract==0.3.13
apscheduler==3.11.0

# ── Module B Enhancement: Ensemble Freshness Engine ──────────────────────────
# Computer Vision pipeline (CPU-only; no CUDA required)
opencv-python-headless==4.10.0.84
scikit-image==0.24.0

# Deep learning (CPU build — avoids CUDA dependency issues on demo machines)
# Install with: pip install torch==2.2.2+cpu torchvision==0.17.2+cpu --index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchvision==0.17.2

# HuggingFace Transformers (CLIP + ViT models)
transformers==4.40.2
huggingface-hub==0.23.2
# Needed by transformers for image processing
timm==0.9.16

# Groq API client (Llama 3.3 70B reasoning layer — optional, graceful fallback)
groq==0.11.0
